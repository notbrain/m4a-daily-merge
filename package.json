{
  "name": "m4a-merge",
  "version": "1.0.0",
  "description": "Merge files based on date into a single file for that day.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Brian Ross",
  "license": "ISC",
  "dependencies": {
    "async": "^2.0.0-rc.3",
    "glob": "^7.0.3"
  }
}
